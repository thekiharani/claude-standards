[
  {
    "query": "we're building the billing side of our saas and i need the actual migrations. plans, subscriptions, invoices, invoice_lines, payments. postgres, alembic, multi-tenant with an org_id on everything. write them.",
    "should_trigger": true
  },
  {
    "query": "here's our schema.prisma (about 20 models). before we ship v1 can you go through it properly and tell me what's going to bite us in a year - keys, indexes, the money columns, the soft deletes, all of it",
    "should_trigger": true
  },
  {
    "query": "adding multi-tenancy to an app that was single-tenant for two years. 34 tables, real customer data in prod. i need a plan and the migrations, and i cannot take downtime",
    "should_trigger": true
  },
  {
    "query": "write the dbmate migrations for a loyalty feature: a points ledger that must never be edited after the fact, redemptions against it, and expiry. raw sql, no orm",
    "should_trigger": true
  },
  {
    "query": "audit our postgres schema against the conventions we agreed and give me a list of what's wrong, ranked by what actually matters",
    "should_trigger": true
  },
  {
    "query": "porting this mysql schema to postgres as part of the move. some of it only works because mysql is lenient - i want it done properly on the other side, not transliterated",
    "should_trigger": true
  },
  {
    "query": "add soft deletes to users, orders and invoices. i know it interacts with our unique indexes somehow and i'd rather you work that out than me find it in prod",
    "should_trigger": true
  },
  {
    "query": "can you look over PR #212, it adds three new tables for the reporting feature. mostly want to know if the keys and the indexes are right before i approve it",
    "should_trigger": true
  },
  {
    "query": "new service from scratch - accounts, api keys, usage records, rate limit buckets. set up the initial schema and migrations the way we do it",
    "should_trigger": true
  },
  {
    "query": "split customers.name into given_name and family_name, backfill the 400k existing rows, and keep the old column readable until the apps have shipped",
    "should_trigger": true
  },
  {
    "query": "this query is doing a seq scan on a 4M row table and taking 8 seconds. `select * from events where payload->>'type' = 'signup' order by created_at desc limit 50`. how do i speed it up",
    "should_trigger": false
  },
  {
    "query": "im getting an N+1 on the orders index page, every order is loading its customer separately. using eloquent, how do i eager load that properly",
    "should_trigger": false
  },
  {
    "query": "our nightly pg_dump has been failing for a week and nobody noticed. want to set up something that actually tells us when a backup hasnt landed",
    "should_trigger": false
  },
  {
    "query": "pgbouncer or the built in pool? we're on a small droplet and hitting max_connections during traffic spikes",
    "should_trigger": false
  },
  {
    "query": "need a seeder that generates about 500 realistic orders across 20 customers so the dashboard doesnt look empty in demos",
    "should_trigger": false
  },
  {
    "query": "designing the graphql schema for our public api. should `Order.lineItems` be a plain list or a proper relay connection with pagination",
    "should_trigger": false
  },
  {
    "query": "i've got a json schema for validating webhook payloads from stripe and its rejecting valid events. the `data.object` bit is typed too strictly i think",
    "should_trigger": false
  },
  {
    "query": "our elasticsearch mapping has `title` as a keyword and search is only matching exact strings. need partial matching but i dont want to reindex 2M docs if i can avoid it",
    "should_trigger": false
  },
  {
    "query": "write me a query that gives monthly recurring revenue by plan for the last 12 months, with the month-over-month change as a percentage",
    "should_trigger": false
  },
  {
    "query": "my postgres container in docker-compose keeps losing data on restart. i think the volume mount is wrong but the syntax looks right to me",
    "should_trigger": false
  }
]
